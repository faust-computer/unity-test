# Unity Test module for Zephyr

### Overview
Using Unity (https://github.com/ThrowTheSwitch/Unity/)

### Setup
To add the driver to your Zephyr project, set `CONFIG_LIBUNITYTEST=y` in your prj.conf, and then run `west update`
